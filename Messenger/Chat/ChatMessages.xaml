<UserControl x:Class="Messenger.Chat.ChatMessages"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Messenger.Chat"
             mc:Ignorable="d" 
             d:DesignHeight="510" d:DesignWidth="450">
    
    
    
    <Grid Background="#1F4768">
        <ScrollViewer VerticalScrollBarVisibility="Visible">
            <ItemsControl HorizontalAlignment="Stretch" Margin="0,0,0,0" x:Name="messages" ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:ChatMessagesItem/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        
    </Grid>
</UserControl>
